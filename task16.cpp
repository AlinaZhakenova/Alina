#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	double a, b, c;
	cin >> a >> b >> c;
	double q, w, e,r;
	q = a * a;
	w = b - 3;
	e = pow(c, 2.0);
	r=(q + w + e);
	cout << r << endl;
	return 0;
}
